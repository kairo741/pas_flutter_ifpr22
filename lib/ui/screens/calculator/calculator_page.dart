import 'package:flutter/material.dart';

import 'calculator_widget.dart';

class CalculatorPage extends StatefulWidget {
  const CalculatorPage({Key? key}) : super(key: key);

  @override
  State<CalculatorPage> createState() => CalculatorWidget();
}
